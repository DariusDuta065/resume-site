{
  "name": "resume-site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.4",
    "autoprefixer": "^10.4.8",
    "concurrently": "^7.3.0",
    "postcss": "^8.4.14",
    "postcss-cli": "^10.0.0",
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "start": "concurrently npm:watch:*",
    "watch:tw": "tailwindcss -i ./assets/css/main.css -o ./assets/css/style.css --watch",
    "watch:hugo": "hugo server",
    "build": "hugo --minify",
    "upload": "yarn build && aws s3 sync ./public s3://dariusduta.dev",
    "deploy": "yarn build && aws s3 sync ./public s3://dariusduta.dev && aws cloudfront create-invalidation --distribution-id E1L08180EI8NA7 --paths \"/*\""
  }
}
